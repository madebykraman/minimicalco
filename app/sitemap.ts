import type { MetadataRoute } from 'next'
import { projects } from './projects/data'
import { services } from './services/data'
export default function sitemap():MetadataRoute.Sitemap{const base='https://minimical.online';const projectUrls=projects.map(project=>({url:`${base}/projects/${project.slug}`,changeFrequency:'monthly' as const,priority:.7}));const serviceUrls=services.map(service=>({url:`${base}/services/${service.slug}`,changeFrequency:'monthly' as const,priority:.65}));const categoryUrls=['brand','digital','campaigns','production'].map(slug=>({url:`${base}/services/category/${slug}`,changeFrequency:'monthly' as const,priority:.65}));return [{url:base,changeFrequency:'weekly',priority:1},{url:`${base}/projects`,changeFrequency:'monthly',priority:.9},{url:`${base}/services`,changeFrequency:'monthly',priority:.85},{url:`${base}/about`,changeFrequency:'monthly',priority:.7},{url:`${base}/contact`,changeFrequency:'monthly',priority:.8},{url:`${base}/projects/drop`,changeFrequency:'monthly',priority:.6},...projectUrls,...categoryUrls,...serviceUrls]}
